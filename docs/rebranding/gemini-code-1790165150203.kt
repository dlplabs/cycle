package com.seuapp.cycle.ui.theme

import androidx.compose.ui.graphics.Color

// Cores Principais da Marca
val DeepPlum = Color(0xFF4A2B4D) // Ameixa Escuro (Substitui o Roxo antigo)
val DeepPlumLight = Color(0xFF6E4572)
val OffWhiteBackground = Color(0xFFF9F7F6) // Fundo super limpo e elegante
val SurfaceCard = Color(0xFFFFFFFF)

// Cores das 4 Fases (Orgânicas e Suaves)
val MenstrualTerracotta = Color(0xFFD07C70) // Terracota Suave
val FollicularSage = Color(0xFF8DB094)      // Verde Sálvia
val OvulatoryPeach = Color(0xFFF4B886)      // Pêssego Dourado
val LutealLavender = Color(0xFFBCA6CE)      // Lilás Lavanda

// Cores de Texto e Ícones
val TextPrimary = Color(0xFF2C2C2C)
val TextSecondary = Color(0xFF707070)